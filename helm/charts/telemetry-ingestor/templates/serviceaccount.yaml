{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "telemetry-ingestor.fullname" .) .Values.serviceAccount.name }}
  namespace: {{ include "telemetry-ingestor.namespace" . }}
  annotations:
{{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
{{- end }}


{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "device-api.fullname" .) .Values.serviceAccount.name }}
  namespace: {{ include "device-api.namespace" . }}
  annotations:
{{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
{{- end }}


{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "mqtt-broker.fullname" .) .Values.serviceAccount.name }}
  namespace: {{ include "mqtt-broker.namespace" . }}
  annotations:
{{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
{{- end }}


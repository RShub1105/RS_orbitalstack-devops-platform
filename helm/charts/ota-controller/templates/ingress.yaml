{{- if (default false (dig "ingress" "enabled" false .Values)) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ota-controller.fullname" . }}
  namespace: {{ include "ota-controller.namespace" . }}
spec:
  ingressClassName: nginx
  rules: []
{{- end }}

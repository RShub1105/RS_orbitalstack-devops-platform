{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "ota-controller.fullname" .) .Values.serviceAccount.name }}
  namespace: {{ include "ota-controller.namespace" . }}
  annotations:
{{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
{{- end }}

